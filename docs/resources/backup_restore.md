---
page_title: "ybm_backup_restore Resource - YugabyteDB Aeon"
description: |-
  Restore a backup to a cluster. All attributes are immutable; changing any attribute triggers destroy and create (a new restore). Use ysql_databases and ycql_keyspaces for selective restore; omit to restore all databases/keyspaces.
---

# ybm_backup_restore (Resource)

Restore a backup to a cluster. All attributes are immutable; changing any attribute triggers destroy and create (a new restore). Use ysql_databases and ycql_keyspaces for selective restore; omit to restore all databases/keyspaces.


## Example Usage

```terraform
resource "ybm_backup_restore" "example" {
  backup_id         = "example-backup-id"
  target_cluster_id = ybm_cluster.example.cluster_id
  use_roles         = false

  ysql_databases = ["yugabyte"]
  ycql_keyspaces = ["example_keyspace"]

  ysql_databases_rename = [
    {
      backup_database  = "yugabyte"
      restore_database = "yugabyte_restored"
    },
  ]

  ycql_keyspaces_rename = [
    {
      backup_database  = "example_keyspace"
      restore_database = "example_keyspace_restored"
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `backup_id` (String) The ID of the backup to restore.
- `target_cluster_id` (String) The ID of the cluster to restore the backup onto.

### Optional

- `use_roles` (Boolean) Restore global YSQL roles. Defaults to false.
- `ycql_keyspaces` (List of String) List of YCQL keyspaces to restore. If empty or omitted, all YCQL keyspaces are restored.
- `ycql_keyspaces_rename` (Attributes List) List of YCQL keyspace renames (backup_database -> restore_database). (see [below for nested schema](#nestedatt--ycql_keyspaces_rename))
- `ysql_databases` (List of String) List of YSQL databases to restore. If empty or omitted, all YSQL databases are restored.
- `ysql_databases_rename` (Attributes List) List of YSQL database renames (backup_database -> restore_database). (see [below for nested schema](#nestedatt--ysql_databases_rename))

### Read-Only

- `account_id` (String) The ID of the account.
- `project_id` (String) The ID of the project.
- `restore_id` (String) The restore operation ID.

<a id="nestedatt--ycql_keyspaces_rename"></a>
### Nested Schema for `ycql_keyspaces_rename`

Required:

- `backup_database` (String) YCQL keyspace name in the backup.
- `restore_database` (String) YCQL keyspace name to use on the restored cluster.


<a id="nestedatt--ysql_databases_rename"></a>
### Nested Schema for `ysql_databases_rename`

Required:

- `backup_database` (String) YSQL database name in the backup.
- `restore_database` (String) YSQL database name to use on the restored cluster.
